import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h3">Página Inicial</Typography>
      <Typography variant="body1">Bem-vindo à página inicial!</Typography>
    </div>
  );
};

export default Home;
